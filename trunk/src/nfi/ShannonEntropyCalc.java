package nfi;

import java.util.HashMap;
import java.util.Map;

public class ShannonEntropyCalc {
	
	public ShannonEntropyCalc(byte[] bytes){
		//supposed to be shannon algorithm
//		double result = 0.0d;
//		for(Byte b : bytes){
//			if(!map.containsKey(b.toString())){
//				map.put(b.toString(), 0);
//			}
//			map.put(b.toString(), map.get(b.toString())+1);
//		}
//		
//		double result = 0.0d;
//		for(String seq: map.keySet()){
//			Double freq = (double) map.get(seq) / bytes.length;
//			result += freq * (Math.log(freq) / Math.log(2));
//		}
	}
}
